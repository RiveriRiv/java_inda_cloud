server:
  port: 8084
spring:
  application:
    name: order
cloud:
  config:
    discovery:
      enabled: true
      service-id: config-service
    fail-fast: true
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8762/eureka